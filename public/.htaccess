Options -MultiViews
RewriteEngine On
RewriteBase /

# Let Apache serve existing files and directories directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback all app routes to the SPA entrypoint.
RewriteRule ^ index.html [L]
